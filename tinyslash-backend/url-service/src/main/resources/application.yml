server:
  port: ${PORT:8080}

# MaxMind GeoIP2 Configuration (for state/city level analytics)
# Sign up free: https://www.maxmind.com/en/geolite2/signup
maxmind:
  account-id: ${MAXMIND_ACCOUNT_ID:}
  license-key: ${MAXMIND_LICENSE_KEY:}
  database-path: ${MAXMIND_DB_PATH:./data/GeoLite2-City.mmdb}
  enabled: ${MAXMIND_ENABLED:true}

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: tinyslash-url-service
  
  # Default profiles if none selected
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # Base MongoDB Config (Specifics in profiles)
  data:
    mongodb:
      auto-index-creation: false
      options:
        max-connection-pool-size: 100
        min-connection-pool-size: 10
        max-connection-idle-time: 600000
        connect-timeout: 10000

  jwt:
    secret: ${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyDoNotUseInProduction}
    expiration: 86400000 # 24h

  # Caching Config
  cache:
    type: ${CACHE_TYPE:redis}

# Actuator for Health Checks
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

# External Service Configurations (Validation Fix)
razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:rzp_test_default}
    secret: ${RAZORPAY_KEY_SECRET:rzp_secret_default}

google:
  client:
    id: ${GOOGLE_CLIENT_ID:google_id_default}
    secret: ${GOOGLE_CLIENT_SECRET:google_secret_default}
  redirect:
    uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/auth/callback}

sendgrid:
  api:
    key: ${SENDGRID_API_KEY:SG.default}
  from:
    email: ${SENDGRID_FROM_EMAIL:noreply@tinyslash.com}
    name: ${SENDGRID_FROM_NAME:Tinyslash Team}

cloudflare:
  api:
    token: ${CLOUDFLARE_API_TOKEN:default}
  zone:
    id: ${CLOUDFLARE_ZONE_ID:0e33fe0b0ba1dbbed0133a1319e078ad}

# Application Configuration
app:
  admin:
    enabled: true
  # Base defaults, overridden by profiles
  frontend:
    url: ${FRONTEND_URL:http://localhost:3000}
  shorturl:
    domain: ${SHORT_URL_DOMAIN:http://localhost:8080}
  
  # Custom Domain Defaults
  domain:
    proxy-target: ${PROXY_TARGET:tinyslash.com}
    ssl:
      provider: ${SSL_PROVIDER:CLOUDFLARE}

slug:
  reserved-words:
    - admin
    - api
    - app
    - auth
    - billing
    - blog
    - careers
    - checkout
    - connect
    - contact
    - dashboard
    - docs
    - download
    - explore
    - faq
    - features
    - feedback
    - forgot-password
    - health
    - help
    - home
    - integrations
    - internal
    - invite
    - jobs
    - legal
    - login
    - logout
    - onboarding
    - pages
    - partners
    - plans
    - pricing
    - privacy
    - profile
    - register
    - reset-password
    - search
    - security
    - settings
    - setup
    - signup
    - sitemap
    - status
    - subscribe
    - support
    - templates
    - terms
    - unsubscribe
    - upgrade
    - verify
    - webhooks
    - widget
    - official
    - verified
    - staff
    - team
    - moderator
    - mod
    - null
    - undefined
    - test
    - example
    - demo
    - sample